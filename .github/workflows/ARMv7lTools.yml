name: Build BusyBox & Dropbear

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential musl-tools git wget

      - name: Set up cross compiler
        run: |
          wget https://musl.cc/arm-linux-musleabi-cross.tgz
          tar xzf arm-linux-musleabi-cross.tgz
          echo "$(pwd)/arm-linux-musleabi-cross/bin" >> $GITHUB_PATH

      - name: Build BusyBox
        run: |
          git clone https://git.busybox.net/busybox
          cd busybox
          make defconfig
          make oldconfig
          sed -i 's/^CONFIG_STATIC[^=]*=.*/CONFIG_STATIC=y/' .config
          make ARCH=arm CROSS_COMPILE=arm-linux-musleabi- -j$(nproc)
          cp busybox ../busybox-armv7

      - name: Build Dropbear
        run: |
          git clone https://github.com/mkj/dropbear
          cd dropbear
          ./configure --host=arm-linux-musleabi --enable-static
          make PROGRAMS="dropbear dbclient dropbearkey dropbearconvert scp" -j$(nproc)
          cp dropbear ../dropbear-armv7

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: compiled-binaries
          path: |
            busybox-armv7
            dropbear-armv7
